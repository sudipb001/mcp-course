def apply_leave(start_date: str, days: int) -> str:
    """Simulates submitting a leave request and returns a confirmation message."""
    return f"Leave request submitted: {days} day(s) starting {start_date}. Awaiting manager approval."
